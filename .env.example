# Secrets and DATABASE_URL live in the sibling backend checkout.
# Symlink the backend .env here, or rely on config.paths (backend package root):
#
#   ln -s ../intotheopen-backend/.env .env
#
# See ../intotheopen-backend/.env.example for the full variable list.

# Optional: invite gate for shared demos
# DEMO_INVITE_PASSWORD=
